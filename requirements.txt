# PlantSwarm runtime dependencies.
# After the Phase 2-5 retirement, the pipeline is:
#   Phase 0   pathome_kb     — Claude-driven canonical KB build (LOCAL)
#   Phase 0R  plantswarm     — Qwen-swarm regional delta extraction
#                              (needs an OpenAI-compatible vLLM endpoint
#                               serving Qwen2.5-VL-7B-Instruct)

# Setup + image cache
numpy>=1.24.0
Pillow>=10.0.0

# Phase 0 — canonical KB (claude -p + Anthropic SDK)
anthropic>=0.40.0
httpx>=0.27.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0

# Config loading
pyyaml>=6.0

# Phase 0R — vLLM server (CUDA-only; install on the GPU host, not the client).
# Uncomment to install where vLLM will serve Qwen:
#   vllm>=0.4.0
#   transformers>=4.40.0
#   torch>=2.1.0
