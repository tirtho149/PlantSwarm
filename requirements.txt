# PlantSwarm dependencies (plant disease diagnosis / multi-agent VLM)

# Core ML / VLM serving
vllm>=0.4.0                   # §4: "served by vLLM (≥0.4.0)"
transformers>=4.40.0
torch>=2.1.0

# Data / parquet
pyarrow>=14.0.0               # parquet I/O (PlantDiagBench-style tables)
pandas>=2.0.0
numpy>=1.24.0
datasets>=2.16.0             # Hugging Face datasets (e.g. LeafBench)
python-dotenv>=1.0.0         # load HF_TOKEN from .env

# Image processing
Pillow>=10.0.0                # PIL Image loading + resizing

# ML / statistics
scikit-learn>=1.3.0           # F1, train_test_split
scipy>=1.11.0                 # Spearman ρ, McNemar, chi2, minimize_scalar
statsmodels>=0.14.0           # MixedLM REML (Appendix D)

# Calibration / conformal
# (implemented in-house: calibration/ece.py, conformal.py, temperature_scaling.py)

# Utilities
tqdm>=4.66.0
pyyaml>=6.0
requests>=2.31.0
autogen-agentchat==0.2.40
autogen-ext[openai]==0.2.40

# Optional: for hedge lexicon PMI (Appendix E)
# nltk>=3.8.1

# Optional: TensorFlow Datasets — Plant Village (see requirements-tfds.txt)
