# Copy to .env and fill in. Do not commit .env.
# Hugging Face (gated datasets, e.g. enalis/LeafBench)
HF_TOKEN=
# Inference URL is set in configs/*.yaml (model.vllm_base_url), not here, unless your own tooling reads it.
